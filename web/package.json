{
  "name": "create-graphql-app-web",
  "version": "0.1.0",
  "main": "src/index.html",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --port=3011",
    "build": "parcel build src/index.html",
    "deploy": "yarn build && gcloud app deploy --project=create-graphql-app"
  },
  "dependencies": {
    "apollo-boost": "^0.3.1",
    "graphql": "^14.2.1",
    "graphql-tag": "^2.10.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.4.3"
  }
}
