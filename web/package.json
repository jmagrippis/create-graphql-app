{
  "name": "create-graphql-app-web",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.tsx",
  "dependencies": {
    "@types/jest": "24.0.15",
    "@types/node": "12.0.8",
    "@types/react": "16.8.21",
    "@types/react-dom": "16.8.4",
    "@types/react-native": "^0.57.63",
    "apollo-boost": "^0.3.1",
    "react": "^16.8.6",
    "react-apollo": "^2.5.6",
    "react-dom": "^16.8.6",
    "react-native-web": "^0.11.4",
    "react-scripts": "3.0.1",
    "reset-css": "^4.0.1",
    "typescript": "3.5.2"
  },
  "scripts": {
    "start": "echo 'use `yarn dev` to start the dev server'",
    "dev": "PORT=3011 react-scripts start",
    "build": "react-scripts build && rm -rf dist && mv build dist",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && gcloud app deploy --project=create-graphql-app"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
